{
    "triggerTaskOnSave.tasks": {
        "Create preview (aghdpl)": [
            "report/src/*.md", // <-- Glob pattern to specify which files should trigger the task
        ],
        "Create preview (pprai)": [
            "report/src/*.md", // <-- Glob pattern to specify which files should trigger the task
        ],
    }
}